server:
  port: 8080
  servlet:
    session:
      cookie:
#        로컬 환경에서만 적용
        same-site: Lax
        secure: false
        http-only: true

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2.client: DEBUG

service-url:
  web: http://localhost:5173
  server: http://localhost:8080

spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres
    username: postgres.acrnnxrusgxmuxaqrafm
    password: tunitgkdl6959**
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 2
      idle-timeout: 5000
      max-lifetime: 20000
      minimum-idle: 1
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        enum:
          storage: STRING
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: KIgRfLvoZUOkvb4ayoba
            client-secret: B8dAOkqe0X
            redirect-uri: "{baseUrl}/login/oauth2/code/naver"
            authorization-grant-type: authorization_code
            scope:
              - name
              - email
            client-name: Naver
            client-authentication-method: client_secret_post
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false  # 애플리케이션 시작 시 자동 실행 방지 (스케줄러로만 실행)
